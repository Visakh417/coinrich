import 'package:flutter/material.dart';

class AppColors{
  /// Here we save all the colors we use in this app
  /// So, for a UI design change or color scheme change,
  /// we have to change the color from here only
  static const Color accentColor = Colors.amber;
  static const Color primaryColor = Colors.black;
  static const Color primaryDarkColor = Colors.black;

  static Color backgroundColor = const Color(0xff363333);

  static const Color text = Colors.white;
  static const Color subText = Colors.white70;
  static const Color caption = Colors.white54;

  static const Color green = Colors.green;
  static const Color red = Colors.red;
  static const Color black = Colors.black;


}